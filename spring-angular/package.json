{
  "name": "springaouth-angular",
  "description": "Spring OAuth Angular Implementation",
  "version": "1.0.0",
  "private": true,
  "keywords": [
    "spring-oauth2"
  ],
  "homepage": "https://localhost:9001/",
  "author": "Mohan Ambalavanan",
  "dependencies": {},
  "devDependencies": {
    "http-server": "^0.7.5",
    "jasmine-core": "^2.2.0",
    "karma": "^0.12.31",
    "karma-jasmine": "^0.3.5",
    "karma-junit-reporter": "^0.2.2"
  },
  "scripts": {
    "prestart": "npm install",
    "start": "http-server app -a localhost -p 9001",
    "test": "karma start tests/karma.conf.js",
    "test-single-run": "karma start tests/karma.conf.js  --single-run"
  }
}
